# This app uses environment variables from the root .env.local file
# Located at: ../../.env.local
#
# If you need to run this app standalone, copy the root .env.local here
# Otherwise, no action needed - Next.js will automatically load from the workspace root

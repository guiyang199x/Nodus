# @knowledge/observability

Redacted logs, traces, metrics, and request/job correlation context.

Implemented in Plan 05. Telemetry must never contain file/chunk/chat bodies, complete prompts, model output, secrets, cookies, or authorization headers.

```ts
import { OBSERVABILITY_PACKAGE } from '@knowledge/observability';
```

/**
 * Redacted logs, traces, metrics, and request/job correlation context.
 *
 * Implemented in Plan 05. Logs must never contain file/chunk/chat bodies,
 * complete prompts, model output, secrets, cookies, or authorization headers.
 */
export const OBSERVABILITY_PACKAGE = '@knowledge/observability' as const;
